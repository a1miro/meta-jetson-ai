# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-jetson-ai"
BBFILE_PATTERN_meta-jetson-ai = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-jetson-ai = "6"

LAYERDEPENDS_meta-jetson-ai = "core"
LAYERSERIES_COMPAT_meta-jetson-ai = "scarthgap"
